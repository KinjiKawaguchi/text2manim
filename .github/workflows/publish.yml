name: publish

# release-please が GITHUB_TOKEN で作るリリースは release イベントを発火させないため、
# 通常経路は release-please ワークフローからの workflow_call。
# release トリガーは手動でリリースを作った場合の保険、workflow_dispatch は再実行用。
on:
  release:
    types: [published]
  workflow_call:
  workflow_dispatch:

jobs:
  pypi:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      # PyPIのTrusted Publishing (OIDC) に必要
      id-token: write
    steps:
      - uses: actions/checkout@v7

      - name: uvをセットアップ
        uses: astral-sh/setup-uv@v7

      - name: ビルド
        run: uv build

      - name: PyPIへ公開
        run: uv publish

  ghcr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/kinjikawaguchi/text2manim
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
